let all=[],filter="all";const grid=document.querySelector("#grid"),statusEl=document.querySelector("#status"),search=document.querySelector("#search");const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));async function load(){statusEl.textContent="Refreshing...";try{const r=await fetch("/api/live",{cache:"no-store"});const d=await r.json();all=d.channels||[];render()}catch(e){statusEl.textContent="Load failed"}}function render(){const q=search.value.toLowerCase();const list=all.filter(x=>(filter==="all"||x.group===filter)&&(`${x.userNick} ${x.title}`).toLowerCase().includes(q));grid.innerHTML=list.map(x=>`<article class="card"><a href="${esc(x.channelUrl)}" target="_blank"><div class="thumb"><img src="${esc(x.thumbUrl)}" loading="lazy"><span class="live">● LIVE</span><span class="viewers">👁 ${Number(x.user||0).toLocaleString()}</span></div><div class="info"><div class="title">${esc(x.title)}</div><div class="nick">${esc(x.userNick)}</div></div></a></article>`).join("");statusEl.textContent=`${list.length} live`}document.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{document.querySelectorAll("nav button").forEach(x=>x.classList.remove("active"));b.classList.add("active");filter=b.dataset.filter;render()});search.oninput=render;document.querySelector("#refresh").onclick=load;load();setInterval(load,60000);